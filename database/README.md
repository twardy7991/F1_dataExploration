1. In order to run the database, simply do `docker compose up`.

2. To navigate the database, connect to container using `docker exec -it f1_db bash`.

3. Then log in to postgres server using `psql -U postgres`, there you can find f1_db database.